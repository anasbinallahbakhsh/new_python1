def reverse_list(l):
    new_list = []

    while len(l) > 0:
        item = l.pop()
        new_list.append(item)

    return new_list

numbers = [1, 2, 3, 4]
print(reverse_list(numbers))


def reverse_list(l):
    new_list=[]

    while len(l) > 0:
     item=l.pop()
    new_list.append(item)
    return new_list
numbers=[1,2,3,4]
print(reverse_list(numbers))

def reverse_list(l):
   new-list=[]
   while len(l) > 0:
    collection=l.pop()
    return 